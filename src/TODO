//ï»¿Une dÃ©pendance entre PluginBase et PluginManager, Ã  discuter
//RÃ©cupÃ©ration du nom du jar : ... le dossier plugins n'est pas fixe....
Les dÃ©pendances cycliques ! Pas encore vÃ©rifiÃ©es.
//Lazy et co : Ã  revoir, pas grand sens pour le runnable par exemple. En fait si.

Validate : ne pas tout quitter, gÃ©rer les exceptions.

CHARGER des bins (class) directement
Exporter dans un dossier le .xml

TOUJOURS utiliser le NOM du PLUGIN (et pas celui du jar...)

//Commencer Ã  implÃ©menter le xml.

JAVADOC

Dependancies = dependencies.

Demander un plugin par type.

Ajout d'un attribut : launch to start.

Ajouter un proxy lors d'une référence vers un objet d'un autre plugin.

Dépendances : mettre tout le groupe dans le même classLoader => plus de findClass. (ou alors pour la gestion du lazy : arbre : classLoaderDeGroupe{ClassLoaders de plugins}

On n'implémente plus IPluginRunnable : ou extend une classe qui l'implémente, et qui contient les méthodes donneMoiLesPluginsQuiImplémentent/ExtendTelTruc.
Contentprovider implémente donneMoiUnObject => lazy là dessus. De base le getObject(nom_du_plugin) renvoie this. (récupère une instance de la classe principale)